FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    OCT_API_URL=http://api:8000

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
COPY ui ./ui
RUN pip install --no-cache-dir .

EXPOSE 8501
CMD ["streamlit", "run", "ui/app.py", "--server.address=0.0.0.0"]
